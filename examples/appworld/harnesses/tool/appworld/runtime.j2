The AppWorld environment is stateful across appworld_execute calls and already provides `apis`; do not import it. Discover exact APIs with keyword-only calls such as `apis.api_docs.show_api_descriptions(app_name="venmo")` and `apis.api_docs.show_api_doc(app_name="venmo", api_name="show_transactions")`, then call application APIs through `apis.<app>.<api>(...)`. Combine related read-only documentation calls, keep output bounded, and avoid Python introspection. Verify mutations with a read API. Finish with `apis.supervisor.complete_task()` or `apis.supervisor.complete_task(answer=...)`; stop only when the tool returns `completed: true`. Treat the tool result's final `responseEnvelope` field as the exact protocol for the next non-tool response, and copy its final `nextResponse` object exactly once when a non-tool response is required. Stop immediately after the object's closing brace; never repeat or concatenate it.
