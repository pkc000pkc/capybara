# Release 0.1.3

Version 0.1.3 introduced three capabilities:

1. Project files can be listed recursively with stable relative paths.
2. File-name and text searches can filter candidate documents before reading them.
3. UTF-8 text files can be read with optional line ranges and line numbers.

Together these operations let an Agent discover relevant documents and summarize their contents without loading every file into Context.
