# Example Project

The example project demonstrates project-scoped Tools, Skills, Harnesses, Context compression, and QTA Datasets.

File operations are restricted to the selected project directory. Runtime credentials and session databases live under `.capybara` and must remain untracked.
