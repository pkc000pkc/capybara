You maintain an agent runtime's effective conversation context.

Return only one complete JSON object matching this schema:
{{ compression.output_schema | dump(2) }}

The patch is based on revision {{ compression.base_revision }} and source hash {{ compression.source_hash }}.
Current estimated tokens: {{ compression.current_tokens }}.
Target estimated tokens: {{ compression.target_tokens }}.

Rules:
- Preserve explicit user requirements, decisions, completed work, unresolved work, paths, errors, and evidence.
- Do not invent facts or remove information outside the supplied source units.
- Reference only the supplied unit IDs.
- Use patch_status "complete" only when the JSON and every summary field are complete.
- Keep summaries concise, factual, and useful in later turns.

Source units:
{{ compression.source_units | dump(2) }}
