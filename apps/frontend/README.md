# Capybara Frontend

Next.js developer interface for inspecting and controlling the Capybara Runtime, project resources, Datasets, Experiments, Context, Tools, Skills, and Harnesses.

Run it from the repository root:

```bash
npm run dev:frontend
```

The interface listens on port `3000` and connects to Backend on port `3005` by default.
