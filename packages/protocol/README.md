# Runtime protocol package

This directory is reserved for the framework-neutral Runtime and Resource protocol types currently mirrored by Backend and Frontend.

The first repository migration deliberately keeps the existing application imports unchanged. Protocol extraction should be a separate reviewed change so package boundaries do not hide behavioral changes in the migration commit.
